import { NewBadget } from './NewBadget';

export const Header = () => {
    return (
        <header>
            <h1>Planificador de Gastos</h1>

            <NewBadget />
        </header>
    );
};
